'use strict';

var fs = require('fs');
var path = require('path');
var util = require('util');
var yeoman = require('yeoman-generator');

var MochaGenerator = module.exports = function MochaGenerator(args, options) {

  yeoman.generators.Base.apply(this, arguments);
  this.sourceRoot(path.join(__dirname, '../../', 'templates'));
  this.appName = this._.trim(this.appname);
  this.className = this._.capitalize(this.appName)

  this.on('end', function () {
    this.installDependencies({
      npm: true,
      skipInstall: options['skip-install']
    });
  });
};

util.inherits(MochaGenerator, yeoman.generators.Base);

MochaGenerator.prototype.setupEnv = function setupEnv() {
  this.copy('_bower.json', 'bower.json');
  this.copy('_package.json', 'package.json');
  this.copy('gulpfile.js', 'gulpfile.js');
  this.copy('gulpfile.coffee', 'gulpfile.coffee');
  this.copy('sample.coffee', 'src/' + this.appName + '.coffee');
  this.copy('sample_demo.coffee', 'src/' + this.appName + '_demo.coffee');
  this.copy('sample_spec.coffee', 'spec/' + this.appName + '_spec.coffee');
  this.copy('testem.mustache', 'spec/testem.mustache');
  this.copy('testem.json', 'testem.json');

  fs.appendFileSync('.gitignore',
    (fs.existsSync('.gitignore') ? '\n' : '') +
    'test/bower_components\n\n' +
    '# Log files generated by Testem\n' +
    'testem.*.json\n' +
    'tests.tap\n' +
    'libpeerconnection.log\n'
  );
};

// ** #TODO: research this **
// ** setupEnv() above is running before this prompt. how to prevent that? **
// MochaGenerator.prototype.askForName = function askForName(){
//   var done = this.async();
//   this.prompt({
//     type: 'input',
//     name: 'name',
//     message: 'Your project name',
//     default: this.appname
//   }, function(answers){
//     this.appname = answers.name.replace(/\"/g, '\\"');
//   done();
//   }.bind(this));
// };
